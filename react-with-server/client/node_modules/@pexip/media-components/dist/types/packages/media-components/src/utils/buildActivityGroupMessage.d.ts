import type { ChatActivityType, ChatActivityMessageParticipants, ChatMessage } from '../types';
export declare const appendGroupedParticipants: (previousMessage: ChatMessage, newMessage: ChatMessage) => ChatActivityMessageParticipants[] | undefined;
export declare const buildGroupedActivityMessage: (activityType: ChatActivityType, previousMessage: ChatMessage, newMessage: ChatMessage) => ChatMessage;
export declare const buildRemoveActivityMessage: ({ meetingId, myIdentity, member, getDisplayName, }: {
    meetingId: string;
    myIdentity: string;
    member: {
        identity: string;
        actor: string;
    };
    getDisplayName: (identity: string) => string;
}) => ChatMessage;
//# sourceMappingURL=buildActivityGroupMessage.d.ts.map