import type { MediaDeviceInfoLike } from '@pexip/media-control';
import type { SelectProps } from '@pexip/components';
export interface MapDevicesToOptions {
    devices: MediaDeviceInfoLike[];
    permissionLabel: string;
    label: string;
    selectedDeviceId: string | undefined;
}
/**
 * Map MediaDeviceInfo to SelectProps['options']
 */
export declare const mapDevicesToOptions: ({ devices, permissionLabel, label, selectedDeviceId, }: MapDevicesToOptions) => SelectProps['options'];
//# sourceMappingURL=devicesSelection.d.ts.map