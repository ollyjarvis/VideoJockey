import type { Signal } from '@pexip/signal';
import type { DeviceStatusInfo } from '../types';
/**
 * Subscribe `onSilentDetected` signal and set the notice info accordingly
 *
 * @returns Tooltip info or `undefined` when it is not applicable
 */
export declare const useAudioDetection: (silentDetectedSubscribe: Signal<boolean>['add'], disableAudioSignalDetection: () => void, stream: MediaStream | undefined) => DeviceStatusInfo | undefined;
//# sourceMappingURL=useAudioDetection.d.ts.map