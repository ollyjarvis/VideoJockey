import type { Dispatch, SetStateAction } from 'react';
export interface Preview<T> {
    preview: T;
    setPreview: Dispatch<SetStateAction<T>>;
    applyChanges: () => boolean;
    hasChanges: () => boolean;
}
export declare const createPreviewHook: <T>({ get, set }: {
    get: () => T;
    set: (value: T) => boolean;
}) => () => Preview<T>;
//# sourceMappingURL=usePreview.d.ts.map