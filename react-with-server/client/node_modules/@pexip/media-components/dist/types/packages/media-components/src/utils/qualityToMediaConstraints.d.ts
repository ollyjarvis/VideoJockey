import { StreamQuality } from '../types';
export declare const qualityToMediaConstraints: (quality: StreamQuality) => {
    width: {
        ideal: number;
        max: number;
    };
    height: {
        ideal: number;
        max: number;
    };
};
//# sourceMappingURL=qualityToMediaConstraints.d.ts.map