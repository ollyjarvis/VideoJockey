import type { UserMediaStatus, UserMediaValidator } from '@pexip/media';
import type { Signal } from '@pexip/signal';
export declare const EASE_DURATION = 500;
export declare const isSpaceKeyboard: (code: string) => boolean;
type PTTStateType = {
    isMuted: undefined | boolean;
    isPressed?: boolean;
    easeMuteTimerId?: number;
};
export declare const initPushToTalkUI: (pushToTalkSignal: Signal<boolean>, pttState?: PTTStateType) => () => void;
/**
 * Unmute call while the space keyboard is pressed. When released, the call is muted again.
 *
 * Remarks: space key default behavior, for example activates button, is disabled.
 */
export declare const createPushToTalkHook: (areBothGranted: UserMediaValidator, isGrantedOnlyAudio: UserMediaValidator, pushToTalkSignal: Signal<boolean>) => (isAudioInputMuted: boolean, setAudioInputMuted: (input: boolean) => void, streamStatus?: UserMediaStatus) => void;
export {};
//# sourceMappingURL=usePushToTalk.d.ts.map