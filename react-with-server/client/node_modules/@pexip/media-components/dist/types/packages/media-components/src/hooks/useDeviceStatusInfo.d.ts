import { UserMediaStatus } from '@pexip/media';
import type { DeviceStatusInfo } from '../types';
interface RequestedMedia {
    audio: boolean;
    video: boolean;
}
export declare const useDeviceStatusInfo: (status: UserMediaStatus | undefined, { audio, video }: RequestedMedia) => DeviceStatusInfo;
export {};
//# sourceMappingURL=useDeviceStatusInfo.d.ts.map