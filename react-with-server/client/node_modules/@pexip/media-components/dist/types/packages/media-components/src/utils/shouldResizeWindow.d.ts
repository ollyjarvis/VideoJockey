/**
 * Returns if the window should be resized for a feature
 * to be available based on a minimum width and height.
 *
 * @param minWidth - The minimum window width for the feature to be available
 * @param minHeight - The minimum window height for the feature to be available
 *
 * @remarks
 * We will use this function to display a message
 * when the window is too small. In this case the window
 * should be made bigger to enable certain features.
 */
export declare const shouldResizeWindow: (minWidth?: number, minHeight?: number) => boolean;
//# sourceMappingURL=shouldResizeWindow.d.ts.map