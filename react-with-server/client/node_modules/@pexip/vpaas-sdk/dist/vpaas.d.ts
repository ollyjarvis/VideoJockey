import type { Vpaas, VpaasSignals, SocketSignals, VpaasConfig } from './types';
/**
 * Creates MEE wrapper for the given `apiAddress` and `userID`
 *
 * @param args - Provides necessary information like `apiAddress` and `userId`
 * @returns Wrapper which encapsulates all interactions with MEE backend including WEBRTC
 */
export declare const createVpaas: ({ vpaasSignals, config, socketSignals, }: {
    vpaasSignals: VpaasSignals;
    config: VpaasConfig;
    socketSignals?: Pick<Required<import("@pexip/socket-manager").SocketSignals<import("@pexip/vpaas-api").VpaasWebsocketMessageTypesUnion>>, "onConnecting" | "onConnected" | "onReconnecting" | "onReconnected" | "onDisconnected" | "onError" | "onMessage"> | undefined;
}) => Vpaas;
//# sourceMappingURL=vpaas.d.ts.map