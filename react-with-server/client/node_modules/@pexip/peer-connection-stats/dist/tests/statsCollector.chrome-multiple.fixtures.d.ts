import type { AnyStats } from '../statsCollector.types';
export type WebRTCStats = AnyStats[];
/**
 * Multiple arrays of stats collected from the same session, with a few seconds
 * between each stats.
 */
export declare const multiStats: WebRTCStats[];
