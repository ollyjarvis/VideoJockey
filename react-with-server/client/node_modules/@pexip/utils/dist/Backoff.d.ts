export declare class Backoff {
    private attempts;
    private ms;
    private max;
    private factor;
    private jitter;
    constructor(opts?: {
        min: number;
        max?: number;
        factor?: number;
        jitter?: number;
    });
    duration(): number;
    promise(): Promise<unknown>;
    reset(): void;
}
