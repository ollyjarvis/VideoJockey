export const createStreamTransformer = (transformer, writableStrategy, readableStrategy) => {
    const transformStream = new TransformStream(transformer, writableStrategy, readableStrategy);
    return transformStream;
};
