import type { Message, SocketSignals } from './types';
import { SocketCloseEventCode } from './types';
export declare const createSocketManager: <T extends Message>(signals: SocketSignals<T>, maxReconnectAttempts?: number) => {
    connect: ({ abortController, url, }: {
        url: string;
        abortController?: AbortController | undefined;
    }) => Promise<unknown>;
    disconnect: (code?: SocketCloseEventCode, reason?: string) => void;
    send: (data: T) => string | undefined;
    readonly readyState: number | undefined;
    readonly bufferedAmount: number | undefined;
};
